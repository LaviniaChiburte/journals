const { Router } = require("express");

const router = Router();

// router.get("/");
// router.post("/");
// router.delete("/:id");
// router.update("/:id");
// router.get("/:id");

module.exports = router;
